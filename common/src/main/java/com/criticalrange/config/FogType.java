package com.criticalrange.config;

/**
 * Custom FogType enum for VulkanMod Extra
 * Replaces the non-existent Minecraft FogType class
 */
public enum FogType {
    WATER,
    LAVA,
    POWDER_SNOW,
    NONE
}
