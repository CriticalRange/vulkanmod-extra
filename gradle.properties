# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true

# Fabric Loom (required for plugin configuration)
loom_version=1.11-SNAPSHOT

# Default Minecraft version (can be overridden with -Pminecraft_version=X.X.X)
minecraft_version=1.21.1

# Mod Properties (these remain constant across versions)
maven_group=com.criticalrange
archives_base_name=vulkanmod-extra